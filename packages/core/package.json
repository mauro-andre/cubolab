{
    "name": "@cubolab/core",
    "version": "0.0.0",
    "private": true,
    "description": "Shared primitives entre os packages do cubolab (paths, state, schemas).",
    "type": "module",
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "exports": {
        ".": "./src/index.ts"
    },
    "scripts": {
        "typecheck": "tsc -p tsconfig.json --noEmit",
        "test": "vitest run --passWithNoTests",
        "build": "tsc -p tsconfig.build.json"
    },
    "engines": {
        "node": ">=22"
    },
    "dependencies": {
        "zod": "^4.3.6"
    }
}
